# Gmail SMTP Configuration
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-digit-app-password

# Email Sender Configuration
# Use format: "Your Name" (with quotes) or leave empty to use just the email
SENDER_NAME="Your Name"

# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:3000

# Note: Replace the values with your actual Gmail and App Password
# To generate an App Password:
# 1. Go to your Google Account settings
# 2. Click on Security
# 3. Under "Signing in to Google", select App passwords
# 4. Generate a new app password for "Mail" and "Other (Custom name)"
# 5. Use the generated 16-character password as GMAIL_APP_PASSWORD
